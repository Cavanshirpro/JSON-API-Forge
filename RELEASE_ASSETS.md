# exampleApps release assets

Download `JSON-API-Forge-exampleApps-v0.5.0` from the successful `Example applications` Action. It contains:

- `JSON-API-Forge-exampleApps-v0.5.0.zip`
- `JSON-API-Forge-exampleApps-v0.5.0.zip.sha256`

The ZIP contains only `app/`, `EXAMPLE_APPS.md`, Bash/PowerShell copy installers and an internal `SHA256SUMS`. Verify the external checksum before attaching the unchanged pair to a GitHub Release.
