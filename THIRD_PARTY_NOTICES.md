# Third-party notices

The base SDK depends on `httpx` and its transitive dependencies. Development/testing uses the tools declared in `pyproject.toml`. The optional `younglion` and `ddm` extras install YoungLion 0.1.x.

These packages are not relicensed under the JSON API Forge Source-Available Self-Host License. Each remains governed by its upstream license. Generate a license inventory from the exact resolved environment before redistribution or compliance review.
