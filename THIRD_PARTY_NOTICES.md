# Third-Party Notices

JSON API Forge depends on third-party Python packages and may interact with external software such as PostgreSQL, MySQL/MariaDB, Redis, MongoDB, Supabase services, Uvicorn and cPanel/Passenger environments.

Those components are **not relicensed** under the JSON API Forge Source-Available Self-Host License. Each third-party component remains governed by its own copyright and license terms.

The dependency declarations in `requirements.txt`, `requirements-dev.txt` and example-specific requirement files are the authoritative package-name inventory shipped with this source tree. Because many requirements intentionally use compatible version ranges, the exact transitive dependency set and exact license texts can vary with installation time and platform.

Before redistributing any independent third-party component, building a container image, or creating a commercial compliance inventory, generate a license report from the exact resolved environment and review the upstream license files for those resolved versions.

This notice does not modify any third-party license.
