# Third-party notices

The branch's generator, installer and deterministic packager use Python/PowerShell/shell standard facilities. Running the examples requires the independently distributed JSON API Forge `main` runtime and its third-party dependencies, each governed by its own upstream license.

Nothing here relicenses third-party components under the JSON API Forge Source-Available Self-Host License. Generate a dependency/license inventory from the exact server environment used for deployment.
