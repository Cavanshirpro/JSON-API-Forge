# PublicCatalog

Secret-free reference for an explicitly public, read-only JSON data source. Filtering and sorting are bounded by allowlists; writes remain disabled. This is the endpoint used by the branch CI smoke test.

API: `GET /api/public-catalog/v1/content/catalog` · Docs: `/api/public-catalog/v1/_docs`
