# TaskBoard

SQL CRUD reference with filtering, search, soft deletion, input schemas and a scoped `task_user` role. After copying this directory, run `forge init`; use the generated `TASK_BOARD_BOOTSTRAP_ADMIN_KEY` once to create durable API keys.

API prefix: `/api/task-board/v1` · Docs: `/api/task-board/v1/_docs`
